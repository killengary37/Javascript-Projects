function myFunction(){   //defining a function and naming it 
    var sentence = "I am learning"; //defining a variable and naming it 
    sentence += "a lot from this book"; // using a variable to concatenate a string
    document.getElementById("Concatenate").innerHTML = sentence; //Putting the value of the variable into the Html ElementFromPoint with the Concatenate id
}
